algoritmo inerr10;

função fazNada (x: inteiro) : lógico
início
	retorne falso;
fim


variáveis
	y	: inteiro;
	z   : lógico;
fim-variáveis

início
	y := 0;
	z := avalia (y,z);// parâmetros a mais
	escreva(z);
fim

